# ============================================================================
# CDP OFFLINE DEPLOYMENT REQUIREMENTS
# This file is used ONLY for offline (air-gapped) pip download targeting
# the CDP Edge Node (Linux manylinux2014_x86_64, Python 3.11 / CPython).
#
# IMPORTANT: pyspark is EXCLUDED here because PySpark is already provided
# by the Cloudera CDH/CDP Spark parcel on the cluster — do NOT install it
# separately as it will conflict with the cluster's managed Spark runtime.
# ============================================================================

# TOML Configuration Parser
tomli>=2.0.1
toml>=0.10.2

# Timezone Utilities
pytz>=2023.3

# Spotify Luigi - Orchestration Engine
luigi>=3.4.0

# Database Connectors & SFTP Driver
oracledb>=1.4.0
pymysql>=1.1.0
psycopg2-binary>=2.9.9
pymssql>=2.2.8
paramiko>=3.4.0

# Cryptography (Paramiko SSH key auth dependency)
cryptography>=41.0.0

# Data File & Excel Feed Processing
pandas>=1.5.3
openpyxl>=3.1.2

# REST API Transport
requests>=2.31.0
urllib3>=2.0.0

# Streamlit Web UI Control Panel
streamlit>=1.30.0
