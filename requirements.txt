# Metadata-Driven PySpark ETL Framework Requirements
# ===================================================

# TOML parsing fallback for Python < 3.11 (Required for Python 3.7-3.10)
tomli>=2.0.0; python_version < '3.11'

# PySpark Engine (Provided by CDP runtime on cluster, required for local dev/testing)
pyspark>=3.2.0

# Management Web UI Control Panel (app.py) & Data Utilities
streamlit>=1.20.0
pandas>=1.3.0

# Unit Testing & CI Verification
pytest>=7.0.0

# CDP Deployment Note:
# When executing on CDP via `spark-submit main.py`, the CDP cluster parcel
# provides PySpark, Hive Metastore connectivity, and JDBC drivers natively.
